import javafx.scene.canvas.GraphicsContext;

/**
 * Handles the game window, rendering and user input.
 * Gets called by Game.startGame().
 *
 * @author Mark
 * @version 1.0
 */
public class GameUI
{
    private Game game;

    public GameUI(Game game)
    {
        this.game = game;
    }

    /**
     * Opens the game window and starts the game loop.
     */
    public void launch()
    {

    }

    /**
     * Handles mouse clicks - plant selection and placing on grid.
     */
    private void handleClick(double mx, double my)
    {

    }

    /**
     * Draws everything on screen each frame.
     */
    private void render(GraphicsContext gc)
    {

    }
}
